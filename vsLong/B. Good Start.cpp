#include <bits/stdc++.h>

using namespace std;

void solve() {
  int w, h, a, b, x1, y1, x2, y2;
  cin >> w >> h >> a >> b >> x1 >> y1 >> x2 >> y2;
  x1 += a, y1 += b;
  int dis1 = abs(x2 - x1), dis2 = abs(y2 - y1);
}

signed main() {
  int t;
  cin >> t;
  while (t--) {
    solve();
  }
}
