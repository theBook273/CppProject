#include <bits/stdc++.h>
#define int long long

using namespace std;

bool valid(int l, int r, int n) { return (0 <= l && r < n); }

void solve() {}

signed main() {
  cin.tie()->sync_with_stdio(0);
  int t;
  cin >> t;
  while (t) {
    solve();
  }
}
