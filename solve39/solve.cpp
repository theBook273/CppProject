#include <bits/stdc++.h>

using namespace std;
using ll = long long;

signed main() {
    long long n, m;

    cin >> n >> m;

    long long a[n][m];

    for (long long i = 0; i < n; i++) {
        for (long long j = 0; j < m; j++) {
            cin >> a[i][j];
        }
    }

    long long x = 0, y = 0;

    for (long long i = 0; i < n; i++) {
        long long maxI = a[x][y];
        for (long long j = 0; j < m; j++) {

        }
    }
}
