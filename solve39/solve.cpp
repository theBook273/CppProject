#include <bits/stdc++.h>

using namespace std;
using ll = long long;

signed main() {
    int n, m;

    cin >> n >> m;

    int a[n][m];

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            cin >> a[i][j];
        }
    }

    int x = 0, y = 0;

    for (int i = 0; i < n; i++) {
        int maxI = a[x][y];
        for (int j = 0; j < m; j++) {

        }
    }
}