#include <bits/stdc++.h>

using namespace std;

long long main() {
    long long a, b;

    cin >> a >> b;

    double temp = (b - a) / 2.0;

    cout << fixed << setprecision(2) << temp << endl;
}
