#include <bits/stdc++.h>

using namespace std;

int a[1000000], st[1000000];

void buildOrReplace(int d, int l, int r, int i, int v) { int m = (l + r) / 2; }

int main() {
    int n;
    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
}