#include <bits/stdc++.h>

using namespace std;

int n, k;

signed main() {
  cin.tie()->sync_with_stdio(0);

  // freopen("SGAME.INP", "r", stdin);
  // freopen("SGAME.OUT", "w", stdout);

  cin >> n >> k;
  string s;
  cin >> s;
}
