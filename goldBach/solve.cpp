#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll solve(long long x) { for (long long i = 2; i <= 1e) }
signed main() {
    long long n;
    cin >> n;

    cout << solve(n) << endl;
}
