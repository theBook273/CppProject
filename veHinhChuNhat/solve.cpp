#include <bits/stdc++.h>

using namespace std;

long long main()
{
    long long n, m;

    cin >> n >> m;

    for (long long i = 0; i < n; i++)
    {
        for (long long j = 0; j < m; j++)
        {
            cout << "*";
        }
        cout << endl;
    }
}
