#include<bits/stdc++.h>

using namespace std;

long long n, m, r, pos[1000000];

int main()
{
    cin >> n >> m >> r;

    for(int i = 0; i < m; i++)
    {
        cin >> pos[i];
    }

    sort(a[i])
}
